import React, { Fragment } from 'react';
import DeviceList from './DeviceList';

const Dashboard = () => {
  return (
    <Fragment>
      <DeviceList />
    </Fragment>
  );
};

export default Dashboard;